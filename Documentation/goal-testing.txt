MEDIA TAGGING MANAGER JELLYFIN PLUGIN — TESTING AND VALIDATION TRACKER
======================================================================

Purpose
-------
This document records every test required to verify Media Tagging Manager
Jellyfin Plugin in a real Jellyfin environment before a public release. It is
separate from project-goals.txt: that file defines what the project must do;
this file records whether each behavior has actually been tested and what
happened.

When test feedback is provided, update the matching item's status and add a
short professional result to the Test result log. Keep failures, unexpected
behavior, workarounds, and retest results documented rather than deleting them.

Status key
----------
[ ] Not tested
[~] Test started, blocked, inconclusive, or failed; follow-up required
[x] Passed in a real Jellyfin server
[-] Not applicable to the tested server or deliberately deferred

Test environment record
-----------------------
Jellyfin version:
Plugin build/version:
Operating system / installation type:
Test server or primary server:
Test library names and approximate item counts:
Selected availability region:
Enabled sources:
Source plans / rate limits relevant to testing:
Date testing began:

0. Source-level Jellyfin 10.11.11 compatibility review
-------------------------------------------------------

[ ] Review the current Jellyfin 10.11.11 compatibility audit and confirm that
    every item marked "live check required" remains unmarked until it has been
    exercised on the real target server.

1. Installation and plugin discovery
------------------------------------

[ ] Build the Release plugin successfully from a clean checkout.
[ ] Confirm the published assembly is named Jellyfin.Plugin.MediaTaggingManager.dll.
[ ] Install the DLL manually in a dedicated Jellyfin plugin directory.
[ ] Restart Jellyfin and confirm the plugin loads without server startup errors.
[ ] Confirm the Dashboard -> Plugins entry displays "Media Tagging Manager".
[ ] Open the plugin dashboard page and confirm all primary and secondary areas are available:
    Main Settings, Network and Provider Settings, Genres and Keywords Settings,
    Collections Tags Settings; Cast and Crew Settings; More Like This Settings;
    Production Companies and Countries Settings; Ratings Settings; Spoken
    Languages and Translations Settings; and Scan as the final secondary-row
    tab.
[ ] Confirm the plugin's existing stable GUID retains settings after replacing
    an older local build with a newer local build.
[ ] Confirm a catalog update retains API credentials, library selections,
    tag selections, regions, automation settings, and Watchmode usage using
    Jellyfin's normal plugin configuration persistence. An update must never
    replace or reinterpret an existing configuration with a different saved
    copy.
[ ] Update a server that has settings from a build containing the former
    Tag Destination(s) setting and confirm the retired configuration fields do
    not reset or prevent loading the remaining saved settings.

Acceptance criteria:
- The plugin loads and its dashboard is reachable without manually editing
  Jellyfin files after installation.
- The server remains usable and reports no plugin-load exception.

2. Library selection and settings persistence
----------------------------------------------

[~] Confirm all configured Jellyfin libraries are listed in Main Settings.
[ ] Select one test library, save, leave the page, and confirm the selection
    persists after reopening the dashboard.
[ ] Restart Jellyfin and confirm the selection persists.
[ ] Select more than one test library and confirm all selected libraries remain
    selected after saving and restart.
[ ] Confirm Save Main Settings saves the plugin's server-wide configuration
    directly and does not offer a misleading per-user or server-name scope
    prompt. Provider and network tags are shared metadata for users who can
    access the selected libraries.
[ ] Clear every selection and confirm the plugin does not interpret this as
    permission to scan every library.
[ ] Confirm an unselected library is absent from Scan controls and is never
    changed by Scan all selected libraries.
[ ] Confirm up to three TMDb availability-country selections save and reload.
[ ] Select three availability countries, save Network and Provider Settings,
    wait for the country list to finish loading, then hard-refresh the dashboard.
    Confirm all three selections remain selected.
[ ] Confirm provider-only mode saves and reloads.
[ ] Confirm network-only mode saves and reloads.
[ ] Confirm combined provider-and-network mode saves and reloads.
[ ] Confirm replacement of plugin-managed tags can be switched on and off.
[ ] With a selected library configured in Jellyfin to save local NFO metadata,
    scan a known title and confirm Jellyfin's normal metadata workflow writes
    the resulting tags to its NFO once.
[ ] With local NFO metadata disabled for a selected library, scan a known title
    and confirm tags update in Jellyfin while the plugin neither creates nor
    directly writes an NFO file.
[ ] Confirm Main Settings, Network and Provider Settings, Logo Settings, and
    Scan Scheduled Tasks Settings each save their intended configuration without
    overwriting unrelated saved settings.
[ ] Save Provider selections with the dedicated button, then use Save Network
    and Provider Settings. Confirm the saved Provider selections remain checked
    after source catalogs redraw and after a hard refresh.
[ ] Repeat the preceding test for Network selections and for Genre selections.
[ ] With saved Provider, Network, and Genre selections, use each tab-level
    Save button without changing that picker. Confirm every saved selection is
    preserved. Then use Select None and save, confirming an intentionally empty
    list is preserved instead of being interpreted as Select All.
[ ] Choose two or three availability countries and a small, distinctive set of
    Provider, Network, and Genre selections. Save each applicable tab, then
    immediately reopen the tab and hard-refresh the dashboard. Confirm the
    exact live checkbox/dropdown values—not a default or Select All state—were
    persisted.
[ ] Confirm each settings save displays the same selections after its immediate
    redraw, after reopening its tab, and after a dashboard hard refresh. Confirm
    scan, backup, manual tag edit, logo deletion, and queued scan controls still
    complete without a JSON-response parsing error.
[ ] Enter the Watchmode account's current usage, then save only API Settings.
    Confirm the saved count persists and that the Watchmode account count does
    not increase because source catalogs were not reloaded.
[ ] Change only the TMDb token or Watchmode API key, save API Settings, and
    confirm the relevant source catalogs refresh. Confirm a Main Settings save
    that does not edit API controls preserves the current quota count.

[ ] After a full scan that adds or removes plugin-managed tags, expand
    Additions and Removals in the Last Scan and confirm only changed selected-
    library media appears, grouped by library. Confirm additions use the saved
    added-tag color and removals use the saved removed-tag color.
[ ] Change both last-scan colors, save, leave and reopen the Scan tab, and
    confirm the saved colors persist and are used by the last-scan view.
[ ] Edit a green or red tag in the last-scan view, save it, and confirm the
    edited tag keeps its original green or red classification. Remove a green
    or red tag and confirm no unrelated tag gains a color or causes an error.
[ ] Confirm the Genres and Keywords Settings tab opens without an error and
    saves its settings without overwriting unrelated configuration.

Acceptance criteria:
- Settings persist across a page reload and Jellyfin restart.
- No unselected library is queried, backed up, or modified.

Cast and Crew Settings
-----------------------

[ ] Open Cast and Crew Settings, save a Cast-only change, leave and reopen the
    page, and confirm the selection persists without changing libraries, API
    credentials, Provider/Network choices, or Genre choices.
[ ] With Add more cast enabled and a deliberately low maximum, run a full scan
    and confirm only missing TMDb cast entries are appended. Confirm existing
    cast names, character names, and ordering remain unchanged.
[ ] Enable selected crew jobs, run a full scan, and confirm only missing people
    from those TMDb jobs are appended while existing crew is unchanged.
[ ] Enable a cast or crew photo option, run a full scan, and confirm its scan
    completion text reports missing people photos, TMDb-profile availability,
    saved-photo count, and storage use.
[ ] Run Scan for Cast and Crew Photos and confirm its Jellyfin-managed progress
    is limited to selected-library Movies and Series and does not add people.
[ ] Before any Cast and Crew scan, expand Cast and Crew Additions and Removals
    and confirm every current Movie and Series in selected libraries appears
    with its current cast, crew, and people who have saved photos.
[ ] After a full Cast/Crew-enabled scan, photo-only scan, and each cleanup
    action, confirm only exact new people/photos are green and only exact
    removals are red. Confirm the next full or photo-only scan replaces that
    color overlay but continues to show all current selected-library items.
[ ] Confirm a saved person photo is shared by Jellyfin across titles for that
    person rather than creating one per-title image copy.
[ ] Run each Cleanup Settings action and confirm it removes only the exact
    credit or image recorded as plugin-created, leaves user/Jellyfin-created
    people metadata untouched, and does not affect an unselected library.

More Like This Settings
-----------------------

[ ] Open More Like This Settings, save Recommendation-only changes, leave and
    reopen the page, and confirm the selection persists without changing
    libraries, credentials, Provider/Network choices, Genre choices, or Cast
    and Crew settings.
[ ] Enable Recommendations and/or Similar Titles, run a normal scan, and
    confirm only selected-library Movies and Series with TMDb IDs receive
    saved direct TMDb relationship records. Confirm no Jellyfin tags, NFO
    fields, media files, posters, or watch-history data are changed.
[ ] Save More Like This Settings, use Load Recommendations and Similar Titles,
    and confirm it queues a dedicated Jellyfin task that only processes saved
    selected-library Movies and Series without running Provider, Network,
    Genre, Keyword, Collection, or Cast/Crew changes.
[ ] Use Update Recommendations and Similar Titles after an initial load and
    confirm it refreshes every saved selected-library record, displays progress
    and a completion summary, adds new TMDb relationship titles, and shows
    removed title relationships in the saved removal color.
[ ] Use Remove Recommendations and Similar Titles and confirm it removes only
    plugin-stored selected-library relationship records, leaves tags/NFO/media
    unchanged, retains the optional poster cache, and does not affect an
    unselected library.
[ ] With Save image links to save space enabled, confirm the overview displays
    returned TMDb posters and the saved relationship data retains poster links
    without filling the local poster cache.
[ ] With Save poster images to disk enabled, confirm cached posters render in
    the overview, each cache entry remains at or below 5 MB, and the displayed
    cache total never exceeds the saved limit.
[ ] Expand Recommendations and Similar Titles Additions and Removals before
    and after a scan. Confirm libraries can be independently collapsed, poster
    aspect ratios remain portrait, new titles use the saved addition color, and
    removed titles use the saved removal color without recoloring unrelated
    titles. With a large relationship result, confirm each library loads only
    after expansion and pagination shows ten source media items at a time
    without blank, lagging, or unresponsive dashboard areas.
[ ] Run Load Recommendations and Similar Titles and verify its completion text
    explicitly reports saved relationship records, items without TMDb IDs,
    TMDb lookup failures, and valid empty TMDb responses. If the overview is
    empty after a prior Load, run Load again after the 0.1.0.61-test repair and
    confirm records are reassociated with their exact saved library and appear
    under that library in the overview. If the overview remains empty, use
    those counts to determine the cause before treating it as a display failure.
[ ] Save Production Companies and Countries Settings with production companies
    and selected production countries enabled, then use Load Production
    Companies and Countries. Confirm it queues a dedicated Jellyfin task limited
    to selected-library Movies and Series, shows progress and a completion
    summary, adds only enabled TMDb values, refreshes the colored production
    overview, and does not run other tag or metadata features.
[ ] With Save Production Company Logos enabled, run the dedicated production
    action and confirm source-supplied company logos enter the bounded cache,
    including when the matching Jellyfin Studio value already existed before
    this plugin.
[ ] Save Production Companies and Countries Settings with production companies
    and selected production countries enabled. Run a normal selected-library
    scan and confirm TMDb production companies appear in Jellyfin Studio
    metadata, selected production countries appear in native production-country
    metadata, and existing values remain present.
[ ] Confirm the Production Country picker is searchable, Select All and Select
    None change only unsaved picker choices, and saving preserves all choices
    after a hard refresh without changing settings from other tabs.
[ ] Confirm production-company logos use no more than one cached file per
    company, respect the existing Main Settings logo-cache limit, appear in the
    production overview when available, and can be removed through the existing
    cached-logo controls.
[ ] Confirm the Production overview is available before a scan, libraries can
    be independently collapsed, latest additions/removals use the saved colors,
    and direct edits update only the chosen selected-library Movie or Series.
[ ] Use each Production cleanup button and confirm only values recorded as
    created by this plugin are removed; pre-existing Jellyfin Studio and
    production-country metadata remains unchanged.

3. Source configuration and credential safety
----------------------------------------------

[ ] Configure a valid TMDb API Read Access Token through the dashboard.
[ ] Configure a valid Watchmode API key through the dashboard.
[ ] Confirm a blank TMDb field disables TMDb without affecting another enabled
    source.
[ ] Confirm a blank Watchmode field disables Watchmode without affecting
    another enabled source.
[ ] Confirm the plugin refuses a manual scan when every source is disabled or
    missing credentials.
[ ] Confirm that refusal leaves existing Provider: and Network: tags unchanged.
[ ] Use an intentionally invalid source credential and confirm a failed lookup
    leaves existing plugin-managed tags unchanged.
[ ] Inspect source code, published DLL directory, backup files, and any private
    manifest used for testing; confirm no real API key is present.
[ ] Confirm source credentials are entered only in the Jellyfin dashboard and
    remain absent from README examples and release files.
[ ] Confirm normal source requests send credentials in HTTP headers rather than
    including them in request URLs, where server-side inspection is possible.
[ ] Confirm a multi-library scan never starts more than 35 TMDb requests in one
    second. With a controlled HTTP 429 test response, confirm all TMDb requests
    temporarily pause, honor a Retry-After value when provided, and retry the
    affected read no more than twice.
[ ] Set a Watchmode request limit and enter the account's **Quota Resets On**
    date. Confirm the visible usage counter identifies the correct 30-day cycle
    (including a reset date of 2026-08-21), does not reset at a calendar-month
    boundary, and stops title and catalog requests at the selected limit.
[ ] Enter an administrator-confirmed **Current API Usage** value (for example,
    106) and save. Confirm the value survives a page reload and that later
    Watchmode requests add to that baseline rather than restarting from zero.

Acceptance criteria:
- Every server uses its own credentials.
- Missing or invalid credentials cannot erase known tags.
- No credential is exposed in project, release, backup, or manifest artifacts.

4. Provider and network tagging behavior
-----------------------------------------

[ ] Scan a title with one known provider and confirm a `Provider: <name>` tag
    is written.
[ ] Scan a title with more than one known provider and confirm all distinct
    providers are retained.
[ ] Scan a TV title with one known network and confirm a `Network: <name>` tag
    is written.
[ ] Scan a title with multiple network values where source data provides them
    and confirm all distinct networks are retained.
[ ] Confirm provider and network tags remain separate on the same title.
[ ] Confirm duplicate source values result in only one case-insensitive tag.
[ ] Confirm provider-only mode writes provider tags without adding networks.
[ ] Confirm network-only mode writes network tags without adding providers.
[ ] Confirm combined mode writes both kinds of tags where source data provides
    them.
[ ] Confirm tags unrelated to the plugin remain untouched after a healthy scan.
[ ] With replacement enabled, confirm stale plugin-owned tags are reconciled on
    a healthy source response while unrelated tags remain untouched.
[ ] With replacement disabled, confirm existing plugin-owned tags are retained
    alongside newly found tags.
[ ] Confirm a source response with no external identifier available does not
    invent a provider or network.
[ ] Confirm the configured availability countries change provider results only
    the source supports; record any expected regional difference.
[ ] Before scanning any media, confirm Select Providers loads the full TMDb
    movie/TV provider catalog for the selected regions and the Watchmode source
    catalog when the corresponding keys are configured.
[ ] Before scanning any media, confirm Select Networks is empty until Load
    Networks has completed for the saved availability regions, while the
    Provider picker remains available normally.
[ ] Click Load Networks and confirm it reports progress, does not scan media or
    change tags, and combines TMDb's country-verified Networks with
    Watchmode's own country-filtered Networks when both sources are configured.
[ ] Change one saved availability region and confirm the prior Network cache is
    not shown until Load Networks is run again for the new saved region set.
[ ] Confirm exact provider spelling aliases are listed and tagged only once:
    `Disney Plus`/`Disney +` as `Disney+`, and `Discovery +` as `Discovery+`.
    Confirm distinct offerings such as Apple TV+ versus a store/rent-buy entry,
    Prime Video versus Amazon rent/buy, and Netflix plan/profile entries remain
    separate choices.
[ ] Confirm Network catalog loading never changes saved Provider/Network
    selections, and a Network-tab save with an unloaded checklist preserves
    the already saved Network selection.
[ ] For a Watchmode fallback TV title, confirm Watchmode's current availability
    is written only as Provider tags and its API-returned `network_names` are
    written only as Network tags.
[ ] With a selected Network that TMDb does not return, confirm a Watchmode
    fallback can still apply a source-returned selected Network. With an
    unselected TMDb Network, confirm it does not suppress that fallback.
[ ] After a scan, confirm the Scan status reports TMDb/Watchmode Network
    returns, Network values filtered by selection, final selected candidates,
    already-present Network tags, Streaming app only exclusions, actual Network
    tags added, and lookup failures.
[ ] Confirm the plugin never creates a Network tag from a TV network streaming
    app name when neither API supplies a title-level network name.
[ ] With TV Network Streaming Apps set to Network only, confirm a Watchmode
    TV-channel-app result does not write a Provider tag and writes only an
    API-returned Network tag.
[ ] With TV Network Streaming Apps set to Streaming app only, confirm a
    Watchmode TV-channel-app result writes its Provider tag and suppresses the
    Network tag for that title.
[ ] With TV Network Streaming Apps set to Both, confirm both selected tags are
    written when the APIs return both values, the streaming app is selected in
    Select Providers, and the network is selected in Select Networks.
[ ] Confirm provider and network pickers show the cached source-supplied logo
    when one exists, and the Provider and Network Library Overview reuses the
    same cached image.
[ ] Confirm the plugin data folder contains no more than one logo file for a
    normalized Provider name and one for a normalized Network name, regardless
    of how many media items use those tags.
[ ] Turn off logo saving/display, save Main Settings, and confirm picker and
    overview logos no longer display or download.
[ ] Click Delete Cached Logos and confirm source-cached and manually uploaded
    logos are removed while all media tags remain unchanged.
[ ] Confirm Logo Settings shows source-logo loading progress, file count, cache
    size, and the configured cache limit.
[ ] Set a smaller cache limit, save Main Settings, and confirm an attempted logo
    load stops safely at that limit without affecting media tags.
[ ] Confirm a source-supplied logo larger than 2 MB is skipped without stopping
    the relevant media-tag scan or logo-load operation.
[ ] Click Load All Logos and confirm it loads source-catalog Provider logos
    and Network logos before any media scan, without changing media tags.
    Confirm only Networks with a documented TMDb mapping and TMDb logo remain
    eligible for a pre-scan Network logo.
[ ] Save a Provider selection, click Load Logos for Selected Providers, and
    confirm only selected-provider logo candidates are processed.
[ ] Use Delete Specific Logos to select one or more cached entries and confirm
    only those logo files are removed while media tags remain unchanged.
[ ] After a selected-library scan, confirm newly discovered provider and
    network values remain listed even if a selection sync removes their tags.
[ ] In Unknown Providers and Networks, use **See Items** for a result and
    confirm its dialog lists only the selected-library Movies or Series with
    that exact unknown Provider or Network tag.
[ ] Confirm Select All and Select None affect every provider checkbox without
    changing network checkboxes, and the equivalent network controls affect
    only networks.
[ ] Search providers and networks independently; confirm each search filters
    only its own list and does not clear existing checkbox selections.
[ ] Save Network and Provider Settings with a large catalog available; confirm
    the Provider and Network picker rows return promptly even while optional
    catalog logos are being cached in the background.
[ ] Confirm selections remain available in those lists after a selection sync
    removes their current tags.
[ ] Select a Provider subset and use Save Provider Selections; confirm it saves
    the future-scan allow-list without scanning media, creating a backup, or
    changing any current tags.
[ ] Select None for Providers, save, and run a scan; confirm no new Provider
    tags are added and existing Provider tags remain unchanged unless the
    explicit Sync with Only Selected Providers action is used.
[ ] Select a Network subset and use Save Network Selections; confirm it saves
    the future-scan allow-list without scanning media, creating a backup, or
    changing any current tags.
[ ] Select None for Networks, save, and run a scan; confirm no new Network
    tags are added and existing Network tags remain unchanged unless the
    explicit Sync with Only Selected Networks action is used.
[x] Select a subset of providers, run Sync with Only Selected Providers, and
    confirm a backup is created, only unselected `Provider:` tags are removed,
    no source request is made, and `Network:`/unrelated tags remain unchanged.
[ ] If a provider or network selection sync cannot run, confirm the dashboard
    displays its specific safe server explanation rather than an unknown-error
    message, and confirm no tags are changed before a preflight failure.
[x] Select a subset of networks, run Sync with Only Selected Networks, and
    confirm a backup is created, only unselected `Network:` tags are removed,
    no source request is made, and `Provider:`/unrelated tags remain unchanged.
[ ] After each selection sync, scan again and confirm only the selected kind's
    chosen values are applied in future scans.

Acceptance criteria:
- Tags are clear, overlap-friendly, and safe to filter in Jellyfin.
- The plugin never treats a missing or failed lookup as a reason to guess or
  remove unrelated metadata.

4a. Genre, keyword, and collection tag behavior
------------------------------------------------

[ ] With a valid TMDb API Read Access Token, confirm the Genre picker loads
    TMDb genres before any media scan.
[ ] Confirm Genre search filters only the Genre list and retains checkbox
    selections.
[ ] Confirm Genre Select All and Select None affect only Genre checkboxes.
[ ] Save a subset of genres, reload the page, and confirm the future-scan
    allow-list persists.
[ ] Scan a known title and confirm only selected source-returned genres are
    added as `Genre: <name>` tags.
[ ] Use Sync with Only Selected Genres and confirm it creates a backup, removes
    only unselected plugin `Genre:` tags, makes no source request, and leaves
    Provider, Network, Keyword, Collection, and unrelated tags untouched.
[ ] Use Remove Genres Added by This Plugin and confirm it creates a backup,
    makes no API request, removes only plugin `Genre:` tags, turns off future
    genre tagging, and preserves every other tag.
[ ] Enable Add keywords during scans before scanning a title with known TMDb
    keywords; confirm `Keyword: <name>` tags are added.
[ ] Disable Add keywords during scans before a scan and confirm no new Keyword
    tags are added.
[ ] Use Remove Keywords Added by This Plugin and confirm it creates a backup,
    makes no API request, removes only plugin `Keyword:` tags, and preserves
    every other tag.
[ ] Scan selected libraries for collections with valid TMDb credentials and
    confirm results are grouped by library and limited to direct TMDb movie
    collection matches.
[ ] Confirm Collection Select All, Select None, and individual checkboxes
    control only the selected collection matches.
[ ] Add selected Collection tags and confirm a backup is created and only
    `Collection: <TMDb collection name>` tags are added.
[ ] Confirm the collection action does not change Provider, Network, Genre,
    Keyword, or unrelated tags.
[ ] In Network and Provider Settings, confirm the Provider and Network Library
    Overview has separate Provider and Network columns and each can be manually
    edited without changing other plugin-managed tag types.
[ ] In Genres and Keywords Settings, confirm the dedicated Library Overview
    has separate Genre and Keyword columns and each can be manually edited
    without changing other plugin-managed tag types.

5. Manual scans, scan status, and performance
---------------------------------------------

[ ] Click Scan all selected libraries and confirm each selected library is
    checked.
[ ] Confirm a single pre-scan backup is created before an all-selected-library
    scan changes tags.
[ ] During a multi-item scan, confirm the dashboard shows running state.
[ ] Confirm the displayed current title changes as items are processed.
[ ] Confirm completed count, total count, and percentage are credible.
[ ] Confirm the ETA appears after sufficient progress and is reasonably close
    on a multi-item test library.
[ ] Confirm the ETA never displays an invalid value such as `NaN`.
[ ] Confirm a completed scan stays visible as a completion result and reports
    checked items, newly added tags, and media items that received a new tag.
[ ] Click Stop Scan and record final status,
    partial tag state, and backup availability.
[ ] Confirm a scan error is shown as a non-fatal status and does not make the
    Jellyfin dashboard unusable.
[ ] Test a representative larger library or larger batch after small-library
    behavior is confirmed; record duration and source-rate-limit behavior.

Acceptance criteria:
- An administrator can tell what is happening during a scan.
- A failed or cancelled scan remains recoverable using backups.

6. New incoming media checks
----------------------------

[ ] Confirm incoming-media checks are off by default in a new configuration.
[ ] Enable incoming-media checks and save settings.
[ ] Run a normal Jellyfin library scan to establish the first enabled baseline.
[ ] Add a new supported movie to a selected test library.
[ ] Run Jellyfin's normal library scan and confirm the new movie is checked and
    tagged after the scan completes.
[ ] Add a new supported TV series to a selected test library.
[ ] Run Jellyfin's normal library scan and confirm the new series is checked
    and tagged after the scan completes.
[ ] Confirm an existing older title is not unnecessarily rechecked by the
    incoming-media path.
[ ] Confirm an automatic pre-change backup is created before an incoming-media
    update changes tags.
[ ] Disable incoming-media checks, add a new supported title, run Jellyfin's
    normal library scan, and confirm no automatic source lookup/tag update runs.
[ ] Confirm manual scans and scheduled full scans still work while
    incoming-media checks are disabled.
[ ] Restart Jellyfin and confirm incoming-media enabled/disabled state and its
    baseline timestamp behave correctly.

Acceptance criteria:
- Incoming media is checked only when the setting is enabled.
- Disabling the setting has no effect on manual or scheduled full refreshes.

7. Scheduled full refresh
-------------------------

[ ] Enable automatic full refresh and set a test interval.
[ ] Restart Jellyfin and confirm Refresh provider and network tags appears in
    Dashboard -> Scheduled Tasks.
[ ] Start the scheduled task manually and confirm it scans configured libraries.
[ ] Confirm the task creates a pre-change backup before changing tags.
[ ] Confirm the task does not scan unselected libraries.
[ ] Confirm disabling automatic full refresh prevents its default recurring
    trigger after restart.
[ ] Change the interval, restart Jellyfin, and confirm the task reflects the
    expected new default schedule.
[ ] Confirm task cancellation behaves safely and leaves a usable backup.
[ ] Record actual schedule behavior, duration, and source rate-limit behavior.

Acceptance criteria:
- Manual, scheduled, incoming-media, combined, and fully disabled automatic
  modes behave independently as configured.

8. Library overview, filters, and manual corrections
----------------------------------------------------

[ ] Open Library overview and confirm supported movies and TV series from
    selected libraries appear.
[ ] Confirm an unselected library's titles do not appear.
[ ] Confirm provider values display correctly.
[ ] Confirm network values display correctly.
[ ] Filter by a complete provider name.
[ ] Filter by partial provider text.
[ ] Filter by a complete network name.
[ ] Filter by partial network text.
[ ] Filter by provider and network together.
[ ] Filter for titles that have at least one provider or network.
[ ] Filter for titles missing both provider and network tags.
[ ] Click Clear Filters and confirm every filter is reset and all matching
    selected-library items return.
[ ] Confirm Library Overview is collapsed by default, expands on request, and
    returns to the collapsed state with the correct control label.
[ ] Edit a title manually to add a provider and network.
[ ] Edit a title manually to remove a provider and network.
[ ] Open Edit from both Library Overview and Additions and Removals in the
    Last Scan. Confirm one tag-edit window shows editable Provider, Network,
    Genre, Keyword, and Collection fields and saves only the intended changes.
[ ] Confirm the manual edit creates a pre-change backup.
[ ] Confirm manual editing preserves unrelated existing Jellyfin tags.
[ ] Confirm the edited title refreshes correctly in the overview.
[ ] Confirm an edit of a missing/deleted item is handled with a useful error.
[ ] Confirm Unknown Providers and Networks lists only prefixed `Provider:` and
    `Network:` tags that are neither plugin-known nor catalog-recognized; confirm
    ordinary Jellyfin tags never appear there.
[ ] Assign an official name to an unknown Provider or Network tag and confirm
    the mapping persists without rewriting the original media tag.
[ ] Upload PNG, JPEG, and SVG logos for mapped unknown names; confirm each is
    displayed where logos are enabled and replaces the prior logo for that name.

Acceptance criteria:
- An administrator can locate, review, and correct supported title tags without
  leaving the plugin dashboard.

9. Backups, restore, and undo
-----------------------------

[ ] Create a named manual backup from settings.
[ ] With no backup available, click Scan All Selected Libraries and confirm the
    dashboard refuses to start the scan and instructs the administrator to
    create a Tag Backup first.
[ ] Confirm the backup list shows its label, timestamp, and item count.
[ ] Restart Jellyfin and confirm the backup remains listed.
[ ] Confirm the saved backup file exists in the plugin data directory.
[ ] Confirm the backup contains complete tag lists, including an unrelated test
    tag not created by this plugin.
[ ] Change provider, network, and unrelated test tags after creating a backup.
[ ] Restore the named backup and confirm all saved tags return exactly to their
    backup state.
[ ] Confirm the restore warning clearly explains that current tags are
    overwritten for saved items.
[ ] Confirm an item removed after backup is skipped without stopping the rest
    of restoration.
[ ] Run a tag-changing scan or manual edit, then use Undo last tag operation.
[ ] Confirm Undo restores the newest pre-change backup as expected.
[ ] Restore an older listed backup and confirm backup selection works, not only
    Undo latest.
[ ] Select a backup and use Delete Backup; confirm only that selected backup is
    removed and no current Jellyfin tags change.
[ ] Click Delete Backup with no backup selected; confirm the dashboard says
    "Select a backup before deleting."
[ ] Test backup and restore with a larger selected library; record duration and
    backup file size.
[ ] Confirm backups remain inaccessible to non-administrator Jellyfin users.

Acceptance criteria:
- A user can safely return selected-library tag state to a known prior backup.
- Backup files survive normal server restarts and never include API credentials.

10. Packaging, catalog manifest, and release safety
---------------------------------------------------

[x] Create a Release ZIP containing the required plugin assembly and release
    files.
[x] Calculate and record the ZIP's real checksum.
[x] Prepare a catalog manifest that points to the real ZIP,
    version, ABI, and checksum.
[x] Confirm the manifest contains no API credentials, backup files, local
    Jellyfin configuration, or private test-media paths.
[ ] Add the current manifest URL to a Jellyfin server.
[ ] Install the plugin from the catalog and confirm installation
    succeeds.
[ ] Restart Jellyfin and confirm the catalog-installed plugin loads normally.
[ ] Test update from one packaged version to a newer one and confirm
    plugin settings remain intact.
[ ] Confirm a user can install from the manifest without any API key, then add
    their own source credentials only through the plugin dashboard.
[ ] Review the public tracked-file set for secrets, local output, DLL build
    output, backups, test artifacts, sessions, and operating-system metadata.
[ ] Review the custom noncommercial license and confirm the repository clearly
    preserves the required project credit and prohibits commercial use without
    written permission.

Acceptance criteria:
- The public installation mechanism works before a stable announcement.
- The release, manifest, and repository contain no user secrets or local data.

Test result log
---------------

Add one entry for every completed, failed, blocked, or retested item. Keep the
test number, date, Jellyfin/plugin version, result, and short evidence.

Template:
YYYY-MM-DD | Test <number/item> | Pass / Fail / Blocked | Jellyfin <version>,
plugin <version> | Brief result and evidence

Entries:
2026-07-21 | Test 2 — library selection | Fail; fix pending retest | Jellyfin
version not recorded, plugin 0.1.0.1-test | The Settings & sources page showed
the Libraries heading but did not populate selectable library checkboxes. The
manual backup action therefore had no selected library scope and provided no
visible response. Fixed in 0.1.0.2-test; retest required.
2026-07-21 | Test 2 — library selection retest | Fail; second fix pending
retest | Jellyfin version and installed plugin version not recorded | The
library list remained absent after the first dashboard lifecycle-event fix.
The plugin has been changed to load settings immediately rather than waiting
for a dashboard page-show event; retest with the 10.11.11-targeted
0.1.0.4-test build is required.
2026-07-21 | Test 2 — library selection retest | Fail; third fix pending
retest | Jellyfin 10.11.11, plugin 0.1.0.4-test | The library list was still
absent and Jellyfin's global loading indicator remained active for more than
five minutes. The plugin now uses its own administrator-only settings and
library endpoint instead of the hanging dashboard-internal request; retest
with 0.1.0.5-test is required.
2026-07-21 | Test 2 — library selection retest | Fail; fourth fix pending
retest | Jellyfin 10.11.11, plugin 0.1.0.5-test | The global loading indicator
was resolved, but the library list still did not display. Jellyfin's official
10.11.11 LibraryStructureController obtains all folders with
GetVirtualFolders(true); the plugin now mirrors that call and handles either
supported API JSON capitalization style. The untested 0.1.0.6-test build was
superseded by 0.1.0.8-test, which adds the documented Dashboard menu entry,
task-manager scan flow, and a fix for the dashboard response wrapper that
reported a missing configuration object.
2026-07-21 | Test 2 — library selection retest | Fail; fifth fix pending
retest | Jellyfin 10.11.11, plugin 0.1.0.8-test | The Dashboard menu entry
appeared, confirming the new assembly loaded. The library list remained absent,
and the page still reported that its settings response lacked a configuration
object. Inspection of the exact Jellyfin Web 10.11.11 source established that
ApiClient.ajax returns an unparsed Response unless JSON parsing is requested.
The plugin now uses the documented ApiClient.getJSON helper for every GET
endpoint. Retest with 0.1.0.9-test is required.
2026-07-22 | Test 5 — scan status | Fail; fix pending retest | Jellyfin
10.11.11, plugin 0.1.0.12-test | During a 221-item scan, the status displayed
“about NaN min remaining.” The dashboard treated Jellyfin's serialized
duration as a numeric value. The next build uses a server-provided numeric
seconds value and must be retested during a multi-item scan.
2026-07-22 | Test 5 — completed scan feedback | Fail; fix pending retest |
Jellyfin 10.11.11, plugin 0.1.0.12-test | The dashboard did not retain a
completion message or report how many tags were added and to how many media
items. The next build records and displays those totals; retest required.
2026-07-22 | Test 8 — Library overview | Fail; fix pending retest | Jellyfin
10.11.11, plugin 0.1.0.12-test | The View Tags overview did not list the
selected library's media after scanning. The overview query now reads each
selected Jellyfin library directly and carries its configured library ID into
the dashboard result; retest required.
2026-07-23 | Test 2 / Test 12 — settings persistence | Fail; recovery fix
pending retest | Jellyfin 10.11.11, plugin version not recorded | A test
catalog update presented a blank plugin configuration, including missing API
credentials and saved selections. Version 0.1.0.35-test adds a server-local
current/previous settings recovery mirror and validates the primary XML before
Jellyfin can replace it with defaults. Retest by updating a configured server
is required.
2026-07-23 | Test 3 / Test 4 — Watchmode usage and catalog scope | Fail; fix
pending retest | Jellyfin 10.11.11, plugin 0.1.0.35-test | Configuration loss
reset the locally tracked Watchmode count even though the Watchmode account
reported 106 requests. The next build adds an editable Current API Usage
baseline. Review of the supplied picker output also found that the Network
picker was receiving Watchmode's entire worldwide catalog; the next build
filters it to the selected availability countries using Watchmode's documented
origin-country value. Retest required.
2026-07-23 | Test 2 / Test 12 — settings persistence regression | Fail;
rework pending retest | Jellyfin version not recorded, plugin
0.1.0.36-test | Updating again cleared both API credentials and every saved
preference. The proposed 0.1.0.37 recovery-copy selection was rejected because
it could restore stale settings rather than the current configuration. The
recovery layer has been removed from the next unissued build. Retest standard
Jellyfin configuration persistence across an update before any further
release.
2026-07-23 | Provider/Network selection, credentials, and logo display |
Fail; fix pending retest | Jellyfin 10.11.11, plugin 0.1.0.38-test | Saving
Main Settings after saving selected Provider/Network choices selected every
picker option. A later refresh also showed blank API credentials. Loading
source logos completed but did not visibly refresh the picker images, and the
completed progress bar remained blue. Version 0.1.0.39-test separates Main
Settings from Provider/Network selections, preserves saved credentials when a
non-credential save posts empty values, adds Save API Settings, and refreshes
logo URLs after a completed load. Retest required.
2026-07-23 | Settings saves and picker-logo display | Fail; fix pending
retest | Jellyfin 10.11.11, plugin 0.1.0.39-test | Provider/Network and
Genres/Keywords saves failed with a generic error because an unset Watchmode
quota-reset date blocked every settings POST. The logo loader completed, but
logos remained absent from pickers. Version 0.1.0.40-test makes the quota date
non-blocking for unrelated settings and preserves Jellyfin's authenticated logo
URL while refreshing cached images. Retest required.
2026-07-23 | Provider/Network and Genre settings persistence | Fail; fix
pending retest | Jellyfin 10.11.11, plugin 0.1.0.40-test | Saving Provider,
Network, or Genre choices still failed after opening the large picker catalogs.
The picker code was adding complete source catalogs to the dashboard
configuration payload. Version 0.1.0.41-test saves only administrator-selected
names and rejects dashboard-supplied catalog arrays. Retest required.
2026-07-23 | Library Overview and manual scan after settings save | Fail; fix
pending retest | Jellyfin 10.11.11, plugin 0.1.0.41-test | The Library
Overview reported zero items before a new scan and Scan All appeared to do
nothing. Full-configuration saves allowed one settings tab to overwrite other
tab state, including saved library selection. Version 0.1.0.42-test uses
server-side per-tab settings updates and shows an immediate queued-scan status.
Retest required.
2026-07-24 | Settings persistence, Library Overview, and full tag scan | Pass |
Jellyfin 10.11.11, plugin 0.1.0.48-test | Main Settings, availability regions,
Provider/Network selections, Genre selections, credentials, and picker logos
persisted through dashboard navigation. Library Overview displayed the selected
library before a new scan. A 221-item full scan completed and added 1,945 tags
across 194 changed media items; the larger count was expected because Genre and
Keyword tagging were enabled in addition to one selected Provider and one
selected Network.
2026-07-24 | Provider/Network selection sync and two-library full scan | Pass |
Jellyfin 10.11.11, plugin 0.1.0.49-test | Sync with Only Selected Providers
and Sync with Only Selected Networks both completed successfully. A subsequent
full scan of two selected libraries completed successfully and the resulting
tagging workflow behaved as expected.
2026-07-25 | Cast and Crew Settings, shared people-photo scan, and overview | Pass |
Jellyfin 10.11.11, plugin 0.1.0.56-test | The dedicated selected-library photo
scan completed for 730 items, found 2,115 missing people photos, found 258
available TMDb photos, and saved all 258 to Jellyfin metadata (3.2 MB). The
Cast and Crew Additions and Removals overview loaded correctly, showed current
selected-library metadata without requiring another scan, and displayed real
Jellyfin person thumbnails beside cast, crew, and people-with-photo names.
2026-07-25 | More Like This selected-library load and production load | Pass;
large-overview rendering follow-up issued | Jellyfin 10.11.11, plugin
0.1.0.61-test | The repaired Recommendations and Similar Titles action checked
730 selected-library items and saved 14,032 recommendations plus 13,415 similar
titles across 702 media records. It reported 26 items without TMDb IDs and two
TMDb lookup failures. The overview ultimately displayed correctly, but creating
all relationship cards at once caused visible dashboard lag and blank areas;
0.1.0.62-test replaces that rendering with per-library, ten-item pagination.
The dedicated Production Companies and Countries action also checked 730 items,
added 509 production companies and 239 production countries across 219 changed
media items, and completed successfully.

Ratings Settings and Spoken Languages and Translations Settings
---------------------------------------------------------------

[x] Save Ratings Settings with Community Rating, Vote Count, classifications,
    and adult flags enabled; verify the primary classification country remains
    blank until intentionally selected and that only this tab's settings change.
[x] Use Load Ratings and Classifications; verify progress, completion feedback,
    native Jellyfin Community Rating/Official Rating where available, and the
    collapsed, per-library Ratings overview with saved color controls.
[x] Save Spoken Languages and Translations Settings with each individual option
    enabled; verify scoped persistence through navigation and reload.
[x] Use Load Spoken Languages and Translations; verify progress, completion
    feedback, and original language, spoken languages, and translation rows in
    the collapsed, per-library overview.
[x] Confirm Ratings Settings retains every checked classification after using
    its bottom Save button, navigation, and reload; confirm the Primary
    Jellyfin Classification Country list is available before choosing checkbox
    countries and that it retains its chosen country automatically.
[x] Confirm Sync with Only Selected Age Ratings and Classifications removes only
    plugin-retained unselected classifications and leaves other rating data.
[x] Confirm Spoken Languages and Translations cleanup buttons independently
    remove only their named data category.

2026-07-25 | Ratings and Spoken Languages/Translations final workflow | Pass |
Jellyfin 10.11.11, plugin 0.1.0.64-test | User confirmed the complete current
workflow works: Ratings country choices and scoped saving, selected
classification sync, Ratings loading/overview, language loading/overview, and
the separate Spoken Languages and Translations cleanup actions. The final tab
layout was also accepted after non-wrapping labels were added.
2026-07-31 | Stable 1.0.0 release preparation | Pass — package validation |
Jellyfin 10.11.11, plugin 1.0.0 | Release build succeeded with zero warnings
or errors. The catalog ZIP contains only
Jellyfin.Plugin.MediaTaggingManager.dll and LICENSE, passed ZIP integrity
testing, and has MD5 0ae85f03836f0197e8c19bb073340ccd. The manifest points to
that exact package. The separately listed live catalog-install checks remain
unmarked until performed on a Jellyfin server.
2026-08-04 | Stable 1.0.0.0 catalog version correction | Pass — package validation |
Jellyfin 10.11.11, plugin 1.0.0.0 | Release build succeeded with zero warnings
or errors. The catalog ZIP contains only
Jellyfin.Plugin.MediaTaggingManager.dll and LICENSE, passed ZIP integrity
testing, and has MD5 46cce625d2b6f385b6b614b4bf633787. The catalog package,
installed manifest, and DLL now use the same four-part version. Live server
confirmation remains required after publication.
2026-08-04 | Stable 1.0.0.1 catalog image-cache repair | Pass — package validation |
Jellyfin 10.11.11, plugin 1.0.0.1 | Release build succeeded with zero warnings
or errors. The catalog ZIP contains only
Jellyfin.Plugin.MediaTaggingManager.dll and LICENSE, passed ZIP integrity
testing, and has MD5 dc69712b501a6d731afc978f2a0ac5f2. Installing this newer
package creates a fresh Jellyfin version folder and local image manifest.
Live server confirmation remains required after publication.
2026-08-05 | Stable 1.0.0.2 Scan tab order | Pass — package validation |
Jellyfin 10.11.11, plugin 1.0.0.2 | Release build succeeded with zero warnings
or errors. The catalog ZIP contains only
Jellyfin.Plugin.MediaTaggingManager.dll and LICENSE, passed ZIP integrity
testing, and has MD5 9bd5e14f94196e3818d7d0a1cafc1fdf. The manifest points to
that exact package. Live server confirmation remains required after
publication.
